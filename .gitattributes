# Keep verified upstream noVNC and raw test-output whitespace.
realms/web/vendor/novnc/** -whitespace
docs/**/*.txt -whitespace
docs/*.txt -whitespace
# Preserve literal context lines in the companion Git patch.
integration/*.patch -whitespace
