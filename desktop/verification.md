# Verification

See [the bundled plugin's verification scope](../README.md#verification-scope). Standalone historical evidence is not proof of this host build. The host tests exercise bundled discovery and toggling; real private desktop startup, input and native viewer behavior must be revalidated after merging the prerequisite and any viewer fixes.
